<div class="finance-empty">
    <i class="{{ $icon ?? 'ri-inbox-line' }} fs-1 d-block mb-2"></i>
    <h5 class="mb-1">{{ $title ?? 'Nothing to show yet' }}</h5>
    <p class="mb-0">{{ $message ?? 'New finance activity will appear here.' }}</p>
</div>
